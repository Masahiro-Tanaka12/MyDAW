# ロードマップ

## 判断基準（全フェーズ共通）

> 「音楽経験ゼロのユーザーが、この機能を見て迷わないか？」
> YES なら実装。NO なら実装しない。

---

## 直感モードの主軸（設計方針）

直感モードは **コード → ドラム → 音質 → メロディ** の4層の逐次選択が主軸。
ホーム画面はコード選択画面そのもので、ムード／ジャンルは生成のトリガーではなく各層の候補を絞り込む任意のフィルタ（チップで選ぶ／選ばない）。テンポのみ例外的に数値スライダーで選べる（詳細: [architecture.md](architecture.md)）。

---

## v0.1.0 ✅ 完成

**テーマ：音が鳴る・作品を持てる**

- [x] ムード選択（元気 / 夜 / 雨 / 春 / おまかせ。以降の候補を絞り込むフィルタ）
- [x] コード進行の選択
- [x] ベース・ドラムの自動生成（重み付き抽選）
- [x] 重み付きランダム抽選による毎回異なる曲
- [x] フェードイン・フェードアウト再生
- [x] マスターコンプレッサー + リミッター
- [x] 曲の保存（LocalStorage）
- [x] マイソング一覧（再生・タイトル編集・削除）
- [x] Mood 別アルバムアートカラー（プレースホルダー）

---

## v0.2.0 ✅ 完成（現在） — 4層選択の完成

**テーマ：「選ぶだけで曲が完成する」体験を4層すべてで実現する**

### コード層（大幅拡張）
- [x] コード進行を度数（ダイアトニックコード理論）ベースに全面移行。キーに依存せず使い回せる`ProgressionTemplate`カタログに刷新（`theory/chordTheory.ts`）
- [x] 「テンプレートから選ぶ」「1コードずつ選ぶ」の2モードを実装。1コードずつモードは度数遷移の重み表（`data/music/theory/transitions.ts`）から次候補を提示
- [x] 汎用テンプレート6種を追加：王道進行／循環進行（50年代進行）／カノン進行／小室進行（哀愁進行）／ブルース進行（セブンスコード）／丸サ進行（セブンス＋借用和音）
- [x] ムード選択を必須の最初の画面から「任意の絞り込みフィルタ（チップ）」に格下げ。ホーム画面はコード選択画面そのものに
- [x] テンポの数値スライダー（60〜180、1刻み）を追加。「設定より選択」ルールの意図的な例外

### ドラム層
- [x] ドラムパターン候補を選択カードとして表示（`getAllDrumOptions`、任意のムードフィルタ対応）
- [x] キック・スネア・ハイハットが揃った7グルーヴに刷新（従来はキックの強弱のみ）
- [x] 選択カードに簡易リズムプレビュー（拍ごとのヒット有無・ベロシティを可視化）を追加
- [x] スネアのベロシティ未反映バグを修正（`Tone.Player`は`start()`にベロシティを渡せないため、`Tone.gainToDb`でdB変換して`volume.value`に反映）
- [x] ドラム4拍グリッドUI（キック/スネア/ハイハット3段×4拍、クリックで「オフ→弱→中→強」）。プリセット選択直後の内容が反映された状態で表示され、その場で試聴できる
- [x] 拍節構造（メトリック・ヒエラルキー）の理論データ（`theory/beatStrength.ts`の`BEAT_STRENGTH`）と、それを参照したヒントテキストの動的生成（`drumHint`）
- [x] グリッドの解像度（1拍=1マス）で再現できないパターン（シンコペーション・8分/16分単位のヒットを含むもの）を`isGridCompatible`で判定し、グリッドをバイパスしてプリセットのまま確定する仕組み

### 音質層（Smart FX Wizard）✅
- [x] 「ズッシリした低音」「スッキリ」「前に出る」の3ラベル → コード/ベース/ドラム/メロディ別`MixConfig`(EQ/コンプ/パン/リバーブ)へのマッピング
- [x] 音質選択カードUI
- [x] 共有リバーブバス（Convolver、Tone.Reverbのクラッシュ回避）・トラック別EQ3/パンの実装

### メロディ層（未着手）
> 方針決定：鼻歌を録音し、既存のピッチ検出ライブラリ（自前でDSPアルゴリズムを実装しない）でメロディに自動採譜する方式で実装する。検出したピッチはコード/キーのスケールに、タイミングはリズムグリッドにそれぞれスナップする（現状 `melodyPattern.notes` は空）
- [ ] マイク録音UI（伴奏を流しながら鼻歌を録音）
- [ ] ピッチ検出ライブラリの選定・導入（AIに依存しないDSPベースのもの。自己相関/YIN系。例: pitchy, pitchfinder等）
- [ ] 検出ピッチのスケール／コードトーンへのスナップ処理
- [ ] タイミングのリズムグリッドへのスナップ処理
- [ ] （検討中）歌うのが苦手なユーザー向けに、候補から選ぶ方式を併用するか

### ベース改善 ✅
- [x] コードルートからの相対音程（度数＋オクターブ）で定義し直し、コード非連動バグを解消
- [x] 進行の小節数（4〜12）に応じてベースパターンをタイリング（繰り返し）する仕組み

### 再生
- [x] 4小節固定・ループなしを解消。`Transport.loop`でコード進行を約45秒分ループ再生

### 楽器プリセット（未着手）
- [ ] ムード別楽器プリセットの追加（現状は全ムード共通の `preset_default`）

---

## v0.3.0 — DAWモード導入（CLAUDE.md Phase3相当）

**テーマ：直感モードで作った曲を、さらに作り込めるようにする**

直感モードとDAWモードは同じ `SongBlueprint` を共有する（CLAUDE.md「コンセプト」参照）。この版でDAWモードへの切り替えを初めて解放する。

### ピアノロール
- [ ] `SongBlueprint.tracks` をノート（横軸：時間、縦軸：音高のブロック）として可視化
- [ ] ノートの長さ・位置・ベロシティの編集
- [ ] **（構想）編集時の説明機能**：ノートやコードを触ると「なぜこの音が鳴っているか」を軽く表示する
  - 例：選んだノートがコードのroot/3rd/5thのどれか、ダイアトニックコード内かどうかをツールチップ的に出す
  - v1.0.0（初心者教育）の解説パネル構想を、ピアノロールでの実操作の文脈に前倒しで組み込むイメージ。常時表示はせず、任意でON/OFFできる補助情報とする
  - 「音楽経験ゼロのユーザーが迷わないか」ではなく「ステップアップ後のユーザーが理論を学べるか」が判断基準（DAWモードの開発ルールに従う）

### トラックミキサー
- [ ] 音量・パン・ミュート・ソロの手動調整
- [ ] 直感モードの自動ミックス値（EQ/コンプ/リバーブ/パン）を初期値として引き継ぐ

---

## v0.4.0 — エクスポート

**テーマ：作った曲を外に持ち出せるようにする**

- [ ] WAV ファイル書き出し（`OfflineAudioContext` を使用）
- [ ] ファイル名はタイトルから自動生成

---

## v0.5.0 — 楽器拡充

**テーマ：音の種類を増やし、ムードの表現力を上げる**

- [ ] コード音色の選択（パッド / ピアノ / ギター）
- [ ] Tone.js Sampler を使った実音源の試験導入

---

## v1.0.0 — 初心者教育

**テーマ：「なぜこの曲が気持ちいいのか」を自然に伝える**

- [ ] 「今流れているのは○○コード進行です」などの解説表示
- [ ] 有名曲との関連付け（例：「王道進行は多くの J-POP で使われています」）
- [ ] 段階的な機能開放（曲数に応じて解説が増える）

---

## 直感モードには実装しない機能

| 機能 | 理由 |
|---|---|
| VST / プラグイン管理 | 初心者には不要、設定コストが高い |
| MIDI 詳細編集 | 専門知識が必要 |
| ピアノロール | 専門的すぎる。v0.3.0（DAWモード導入）で実装する |
| 複数トラックの手動ミキサー | 自動で十分。v0.3.0（DAWモード導入）で実装する |
| 曲中のテンポ変動（ritardando 等）| 初心者には高度。曲全体の初期テンポ選択（スライダー）とは別の話 |

## 全バージョン共通の禁止事項

| 機能 | 理由 |
|---|---|
| AI による曲生成 | データベース駆動の方針を維持する |
